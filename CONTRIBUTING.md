# 提交插件

## 流程

1. **做插件**：照 [`docs/PLUGIN-AUTHORING.md`](docs/PLUGIN-AUTHORING.md)。插件目录（manifest.json、板块声明、脚本、README.md、可选 preview.json）放进**你自己的 GitHub 仓库**，打一个 tag（如 `1.0.0`；monorepo 建议 `plugin-<id>-1.0.0`）。上架后不要删除或改写这个 tag——索引钉死了它的文件哈希，改写等于自我下架（注册表切到镜像并标注「作者仓库已失联」）。
2. **写条目**：fork 本仓库，新建 `plugins/<id>.json`：

   ```json
   {
     "id": "my-plugin",
     "repo": "https://github.com/you/my-plugin",
     "tag": "1.0.0"
   }
   ```

   monorepo 加 `"path": "plugins/my-plugin"`。**哈希不用写**——CI 按 tag 拉取内容自动计算。`official` 与 `reviewedAt` / `reviewTier` 由审核方在合入时补，作者不填。
3. **提 PR**。CI 跑 `scripts/build_index.py --check`（结构校验 + 哈希 dry run）；然后进入人工审核。
4. **合入即上架**。CI 重建 `index.json` 并把内容备份进 `mirror` 分支。

**更新** = 打新 tag + 提 PR 把条目的 `tag` 换成新的，`version` 必须递增，**每个版本从零重新过审**。用户侧更新后板块会自动停摆等待重新批准——这是机制保证的，不要试图「让更新无感」。

**下架** = 从 `plugins/` 移除条目（新装与更新立即关闭）。已装用户的板块不会被远程停用——注册表没有、也不会有反向控制通道。

## 审核：义务、分级与 SLA 的诚实表述

**审核是渠道义务。** 每个上架版本在合入前都会被审一次：CI 查机器能查的，人工通读机器查不了的。清单全文公开在 [`docs/REVIEW-CHECKLIST.md`](docs/REVIEW-CHECKLIST.md)——作者知道会被怎么审，照着自查能省一轮往返。

**快慢两线**，按披露与 CI 结果机械判定，不靠感觉：

| 线 | 判定条件 | 谁审 |
| --- | --- | --- |
| **快线** | 披露为纯本地只读：`network` 与 `writes` 为空、不碰凭据存储；CI 全绿 | Agent 审核流水线通读 + 人工抽查 |
| **慢线** | 披露含网络、凭据、写文件或系统控制，或 CI 标记可疑 | 人工逐行通读 |

想走快线，就把插件做成不需要网络的样子——这个激励方向是刻意的。

**SLA 的诚实版本**：

- 审核是**尽力而为的人工通读，不是形式化验证，不是沙盒，也不是担保**。它防「来路不明」，防不了通读读不出来的定向恶意——那一层由 Sono 客户端的逐命令授权、哈希钉死与执行审计兜底。
- 没有承诺的审核时限。积压时**宁积压，不降标准**——排队的作者看得到这句话，也是这句话在保护你的插件所在的这个渠道。
- 发现漏审的处置：下架 + 在原 PR 留档说明，必要时向用户发 alert 级资讯。不悄悄处理。

## 拒收线（不是劝改，是拒收）

- 混淆或压缩过的代码；二进制（脚本必须是可读文本）
- 网络端点与披露不符；读取披露之外的凭据存储
- 要求提权
- argv 经 shell（`sh -c` / `bash -c`）
- 运行时下载并执行代码（`network` 披露只许出现在「读数据」的语境）
- 密钥进 argv（执行审计记录完整 argv）

## 其他

- `id` 先到先得，但与既有插件近似到足以误导的名字（如蹭官方插件名）会被拒。
- `official: true` 只有官方提交能加，CI 校验提交者。
- 商店不展示安装量、star、评分——信任来源是披露、命令全文与可读的脚本，不是社交证明。
